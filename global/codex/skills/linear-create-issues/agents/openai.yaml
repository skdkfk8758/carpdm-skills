interface:
  display_name: "Linear 이슈 생성기"
  short_description: "기존 맥락을 확인하고 Linear 이슈를 안전하게 생성"
  default_prompt: "Use $linear-create-issues to inspect existing Linear context, preview a human-readable issue, and create it after approval."
