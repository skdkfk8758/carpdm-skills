# ADR <NNNN> — <제목>

- Status: Proposed
- Date: <YYYY-MM-DD>

## Context

<무엇이 문제인가. 관측된 사실·수치·이슈 ID. 추측은 "추측:" 이라고 표기한다.>
<`/council-research` 를 돌렸으면 그 결론과 출처를 여기로 옮긴다.>

## Decision

<무엇을 하기로 했는가. 한 문단 + 필요하면 번호 목록.>
<고려했지만 버린 대안과 버린 이유를 한 줄씩.>

## Consequences

<이 결정으로 좋아지는 것과 감수하는 것. 되돌릴 때의 비용.>

## Steps

스텝당 **파일 3개 이하 · 커밋 1개 · 검증 명령 1개 이상**. 3~7개로 끊는다.

- [ ] 1. <목표> — 파일: `<path>` · 검증: `<VERIFY-CMD-1>`
- [ ] 2. <목표> — 파일: `<path>` · 검증: `<VERIFY-CMD-2>`
- [ ] 3. <목표> — 파일: `<path>` · 검증: `<VERIFY-CMD-3>`

## Verification

- `<VERIFY-CMD-1>` → <실제 종료코드·수치>
- `<VERIFY-CMD-2>` → <실제 종료코드·수치>
- 리뷰: `/council-review <대상>` → <판정과 지적 요약>
