# skill-creator eval scratch (fixtures contain nested .git; not a deliverable)
*-workspace/
sweep-workspace/

# skill-creator eval scaffolding inside a skill dir — not part of the shipped skill
skills/*/evals/

# Python bytecode
__pycache__/
*.pyc

# Claude Code 런타임 스케줄러 lock — deliverable 아님
.claude/scheduled_tasks.lock

# graphify 생성물 — 소스 문서에서 재생성, deliverable 아님
graphify-out/
