# skill-creator eval scratch (fixtures contain nested .git; not a deliverable)
*-workspace/
sweep-workspace/

# skill-creator eval scaffolding inside a skill dir — not part of the shipped skill
skills/*/evals/
